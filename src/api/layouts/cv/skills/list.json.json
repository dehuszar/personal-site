{{ define "response" }}
{
    "title": "{{ .Title }}",
    "content": {{ trim ( .Content | jsonify | safeJS ) "\n" }},
    "items": [
        {{ range $index, $e := .Data.Pages }}
        {{ if $index }}, {{ end }}{{ .Render "skill" }}
        {{ end }}
    ]}
{{ end }}