baseURL = 'https://samuel-allen.com/'
languageCode = 'en-us'
paginate = 10000
title = 'Samuel deHuszar Allen'
theme = "blank"

[permalinks]
  posts = '/posts/:year/:month/:title/'
  skill = '/cv/skills/:slug'

[taxonomies]
  category = 'categories' 
  skill = 'skills'
  tag = 'tags'

[outputs]
  home = ['HTML', 'JSON']
  music = ['HTML', 'JSON', 'RSS']
  page = ['HTML', 'JSON']
  posts = ['HTML', 'JSON', 'RSS']
  section = ['HTML', 'JSON']
  taxonomy = ['HTML', 'JSON']
  term = ['HTML', 'JSON']
